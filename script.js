//your JS code here. If required.
 window.addEventListener('DOMContentLoaded', function() {
            let pElement = document.createElement('p');
            pElement.innerText = 'DOM load success';
            document.body.appendChild(pElement);
        });
